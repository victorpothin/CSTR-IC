function [fault] = fault_cont(phi, philim, base_complete)

fault_moment = [];
for i = 1:size(base_complete,1)
    if phi(i) >  philim
         fault_moment = i;
         break
    end
end
return 

end

